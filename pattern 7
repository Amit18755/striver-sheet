class Solution {
  public:
    void printTriangle(int n) {
         int row, sp, d=0;
  for(row=1; row<=n; row++){
      for(sp=1; sp<=(n-row); sp++)
      cout<<" ";
      while(d!=2*row-1){
          cout<<"*";
          d++;
      }
      d=0;
      cout<<"\n";
  }
    }
};
